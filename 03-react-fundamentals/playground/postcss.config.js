// PostCSS processes the CSS through a chain of plugins.
// Tailwind uses PostCSS to generate utility classes from your config.
// Autoprefixer adds vendor prefixes for browser compatibility.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
